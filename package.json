{
  "name": "@time-with/spinner",
  "version": "1.1.0",
  "description": "React SVG-based animated spinner",
  "main": "dist/index.js",
  "license": "MIT",
  "author": "Antonio Brandao <antoniobrandaodesign@gmail.com>",
  "scripts": {
    "prepack": "yarn && babel src --out-dir dist --source-maps && rm -rf node_modules",
    "postpack": "rm -rf node_modules"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1"
  },
  "peerDependencies": {
    "@emotion/core": "^10.0.7",
    "@emotion/styled": "^10.0.7"
  },
  "babel": {
    "presets": [
      "env",
      "react"
    ]
  }
}
